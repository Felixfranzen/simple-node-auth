const storage = require('../../storage/index');

describe('Storage', () => {
  test('should prevent missing parameters when creatin a user', async () => {
    //const res = await storage.saveUser('','');

  });
});